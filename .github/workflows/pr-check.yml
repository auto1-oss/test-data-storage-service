name: PR check test-data-storage

on:
  pull_request:
    branches-ignore:
      - main

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run PR check for test-data-storage
        run: mvn test
